#include <stdio.h>
#include <stdlib.h>

/**
 * main - function that prints the name of the file
 *
 * Return: return 0 always upon succes
 */

int main(void)
{
	printf("%s\n", __FILE__);
	return (EXIT_SUCCESS);
}
