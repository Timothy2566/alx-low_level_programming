#include <stdio.h>

/**
 * main - main fuction for input and out put
 *
 * Return: alwways 0 except otherwise
 */
int main(void)
{
	int i;

	for (i = 0 ; i < 10 ; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
