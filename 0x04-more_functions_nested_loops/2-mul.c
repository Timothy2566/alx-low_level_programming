#include "main.h"

/**
 * mul - its multiplies two integers
 *
 * @a: the first function parameter to multiply
 *
 * @b: the second number parameter to multiply with d first
 *
 * Return: the resturn results between a and b
 */
int mul(int a, int b)
{
	return (a * b);
}
